# Задача: Переход на deepvk/USER-bge-m3 через HuggingFace API

## Цель
Улучшить качество поиска по русскому сленгу/мату, заменив OpenAI text-embedding-3-small на русскоязычную модель deepvk/USER-bge-m3.

## Проблема
Текущая модель OpenAI плохо понимает русский сленг:
- "анал" похож на "понял" (sim=0.75) больше чем на "попа" (sim=0.68)
- Фонетическое сходство важнее семантического
- Модель обучена на формальных текстах, не понимает контекст

## Что нужно сделать

### 1. Добавить поддержку HuggingFace Inference API в EmbeddingClient

**Формат запроса HuggingFace:**
```http
POST https://api-inference.huggingface.co/models/deepvk/USER-bge-m3
Authorization: Bearer hf_xxxxx
Content-Type: application/json

{"inputs": ["текст 1", "текст 2"]}
```

**Формат ответа:**
```json
[[0.123, -0.456, ...], [0.789, -0.012, ...]]
```

Отличия от OpenAI:
- Другой URL
- Другой формат ответа (просто массив векторов, без обёртки)
- Нет поля `model` в запросе
- Размерность: 1024 (вместо 1536)

### 2. Изменения в конфигурации

```json
{
  "Embeddings": {
    "Provider": "huggingface",
    "ApiKey": "hf_xxxxx",
    "BaseUrl": "https://api-inference.huggingface.co/models/deepvk/USER-bge-m3",
    "Dimensions": 1024
  }
}
```

### 3. Изменения в коде

**EmbeddingClient.cs:**
- Добавить поддержку разных провайдеров (OpenAI, HuggingFace)
- Парсить ответ HuggingFace (массив векторов напрямую)
- Убрать поле `model` из запроса для HuggingFace

**DatabaseInitializer.cs:**
- Размерность вектора: 1024 вместо 1536
- Нужна миграция таблицы `message_embeddings`

### 4. Снизить минимальную длину сообщения для embedding

**MessageStore.cs** (строка ~180):
```csharp
// Было:
AND LENGTH(m.text) > 10

// Стало:
AND LENGTH(m.text) > 5
```

Это позволит индексировать короткие сообщения типа "сосун", "анал" и т.п.

## Требуется ли реиндекс?

**ДА, полный реиндекс обязателен:**

1. **Размерность изменилась**: 1536 → 1024
   - Нельзя смешивать векторы разной размерности
   - PostgreSQL выдаст ошибку при сравнении

2. **Семантика изменилась**:
   - Векторы от разных моделей несовместимы
   - Даже если размерность совпадает — нельзя сравнивать

### План миграции:

```sql
-- 1. Удалить старые embeddings
TRUNCATE message_embeddings;

-- 2. Пересоздать таблицу с новой размерностью
DROP TABLE message_embeddings;
-- DatabaseInitializer создаст с новой размерностью при старте

-- 3. Запустить фоновую индексацию
-- BackgroundEmbeddingService проиндексирует всё заново
```

**Время реиндекса:**
- ~88,000 сообщений в базе
- ~100 сообщений/мин (с учётом rate limits HuggingFace)
- **~15 часов** на полную переиндексацию

**Вариант ускорения:**
- HuggingFace Inference Endpoints ($0.06/час) — без rate limits
- Или self-hosted TEI на сервере

## Нужно ли останавливать бота на время реиндекса?

### Короткий ответ: НЕТ, но поиск будет работать плохо

**Что будет работать:**
- Приём и сохранение новых сообщений ✅
- Команда /summary ✅
- Админские команды ✅

**Что будет работать ПЛОХО:**
- /ask, /search, /recall, /factcheck — вернут мало результатов или ничего
- Постепенно будут появляться результаты по мере индексации

### Варианты действий:

#### 1. Ничего не делать (рекомендуется)
- Бот работает как обычно
- Поиск временно деградирован
- Через 15 часов всё восстановится
- Пользователи могут заметить, что поиск "тупит"

#### 2. Отключить поисковые команды временно
Добавить флаг в `AdminSettingsStore`:
```csharp
// В начале SearchHandler, AskHandler, RecallHandler, FactCheckHandler:
if (await _adminSettings.GetAsync("search_disabled") == "true")
{
    await botClient.SendTextMessageAsync(chatId,
        "🔄 Поиск временно недоступен — идёт переиндексация. Попробуй через пару часов.");
    return;
}
```

Включить/выключить через /admin:
```
/admin set search_disabled true
/admin set search_disabled false
```

#### 3. Остановить бота полностью
```bash
docker stop watchmenbot-app
# ... ждём 15 часов ...
docker start watchmenbot-app
```
**Минус:** Потеряем все сообщения за это время!

#### 4. Запустить отдельный инстанс для индексации
```bash
# Основной бот работает со старыми embeddings
# Отдельный процесс делает реиндекс в новую таблицу
# После завершения — переключаем
```
**Сложно, но zero-downtime**

### Рекомендация

**Вариант 1** — просто запустить реиндекс и подождать. Поиск будет постепенно улучшаться. Можно написать в чат:

> "Обновляю поиск, следующие ~15 часов он будет работать хреново. Потом станет лучше понимать русский мат 🔥"

---

## Лимиты HuggingFace Inference API

**Бесплатный тариф:**
- ~1000 запросов/день
- Rate limit: ~10 req/min
- Модель может "засыпать" (cold start ~30 сек)

**Inference Endpoints (платно):**
- $0.06/час за dedicated instance
- Нет rate limits
- Всегда "горячая" модель

## Чеклист

- [ ] Добавить `Provider` в конфиг Embeddings
- [ ] Реализовать HuggingFace формат в EmbeddingClient
- [ ] Изменить размерность в конфиге на 1024
- [ ] Снизить минимальную длину с 10 до 5 символов
- [ ] Добавить миграцию для пересоздания таблицы embeddings
- [ ] Получить HuggingFace API ключ
- [ ] Протестировать на нескольких сообщениях
- [ ] Запустить полный реиндекс
- [ ] Проверить качество поиска на "сосун", "анал" и т.п.

## Альтернативы

Если HuggingFace не подойдёт:
1. **Jina AI** — OpenAI-совместимый API, $0.02/1M токенов, хороший multilingual
2. **Self-hosted TEI** — бесплатно, но нужен сервер с GPU/хорошим CPU
3. **Voyage AI** — лучшее качество multilingual, $0.12/1M токенов
