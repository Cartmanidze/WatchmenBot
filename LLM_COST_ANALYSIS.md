# Анализ стоимости LLM для WatchmenBot

> **Актуально на: 17 декабря 2025**
> **Курс ЦБ РФ: 79.43₽ за $1**

---

## Сравнение моделей (декабрь 2025)

### По цензуре и стоимости

| Модель | Цензура | Input (₽/1M) | Output (₽/1M) | Контекст | Рекомендация |
|--------|---------|--------------|---------------|----------|--------------|
| **DeepSeek V3.2-Exp** | 🟢 Минимальная | 22₽ | 33₽ | 128K | ✅ **Лучший выбор** |
| **Qwen Turbo** | 🟢 Низкая | 4₽ | 17₽ | 128K | Самая дешёвая |
| **Mistral Devstral Small** | 🟢 Низкая | 8₽ | 24₽ | 32K | Хорошая альтернатива |
| Mistral Medium 3 | 🟢 Низкая | 32₽ | 159₽ | 32K | Дороже, но умнее |
| GPT-4o Mini | 🟡 Средняя | 12₽ | 48₽ | 128K | Надёжный fallback |
| Grok 4 Fast | 🔴 **Высокая** | 16₽ | 40₽ | 128K | ❌ 97-99% отказов |
| Gemini 2.5 Flash | 🔴 Высокая | 12₽ | 48₽ | 1M | ❌ Будет резать |
| Claude Haiku 3 | 🟠 Выше среднего | 20₽ | 99₽ | 200K | Дорого для задачи |
| Claude Haiku 4.5 | 🟠 Выше среднего | 79₽ | 397₽ | 200K | Слишком дорого |

### Embedding модели (для RAG)

| Модель | Цена (₽/1M токенов) | Размерность | Качество RU |
|--------|---------------------|-------------|-------------|
| **text-embedding-3-small** | 1.6₽ | 1536 | ✅ Хорошее |
| text-embedding-3-large | 10₽ | 3072 | ✅ Отличное |
| BGE-M3 (self-host) | Бесплатно | 1024 | ✅ Хорошее |

---

## Почему DeepSeek V3.2-Exp?

1. **Минимальная цензура** — не режет маты, шутки, спорные темы
2. **Отличный русский** — одна из лучших моделей для RU
3. **Дёшево** — 22₽/33₽ за миллион токенов
4. **Умная** — 671B параметров (MoE), сравнима с GPT-4o
5. **128K контекст** — достаточно для RAG
6. **Cache hits** — можно снизить input до 2₽/1M (экономия 90%)

### Важно про Grok 4 Fast

Несмотря на низкую цену, **Grok 4 Fast НЕ подходит**:
> В тестах безопасности Grok 4 Fast отклонил 97-99% "нежелательных" запросов.
> Модель использует жёстко зашитый safety prompt.

---

## Расчёт для 5 групп Telegram

### Исходные данные

| Параметр | Значение |
|----------|----------|
| Количество групп | 5 |
| Сообщений на группу в день | 150 (средняя активность) |
| Токенов на сообщение | ~50 |
| Дней в месяце | 30 |
| **Всего сообщений в месяц** | 22,500 |

---

### 1. Ежедневные саммари (Daily Summary)

```
Вызовов LLM: 5 групп × 30 дней = 150 вызовов/месяц

Каждый вызов:
├── Input:  150 сообщений × 50 токенов + system prompt ≈ 8,000 токенов
└── Output: саммари ≈ 800 токенов

Итого в месяц:
├── Input:  150 × 8,000  = 1,200,000 токенов (1.2M)
└── Output: 150 × 800    = 120,000 токенов (0.12M)
```

### 2. RAG запросы (опционально)

```
Предположение: 10 вопросов в день на все группы
Вызовов: 10 × 30 = 300 запросов/месяц

Каждый запрос:
├── Input:  запрос + 10 chunks контекста ≈ 3,000 токенов
└── Output: ответ ≈ 500 токенов

Итого в месяц:
├── Input:  300 × 3,000 = 900,000 токенов (0.9M)
└── Output: 300 × 500   = 150,000 токенов (0.15M)
```

### 3. Embeddings (для RAG)

```
Сообщений: 5 групп × 150 сообщений × 30 дней = 22,500 сообщений
Токенов:   22,500 × 50 = 1,125,000 токенов (1.1M)
```

---

## Итоговая стоимость в месяц

### DeepSeek V3.2-Exp (рекомендуемый)

| Компонент | Input | Output | Стоимость |
|-----------|-------|--------|-----------|
| Daily Summary | 1.2M × 22₽ | 0.12M × 33₽ | 26₽ + 4₽ = **30₽** |
| RAG queries | 0.9M × 22₽ | 0.15M × 33₽ | 20₽ + 5₽ = **25₽** |
| Embeddings | 1.1M × 1.6₽ | — | **2₽** |
| **ИТОГО** | | | **57₽/месяц** |

### С использованием кэша (cache hits)

При правильной структуре промптов можно получить 80%+ cache hits:

| Компонент | Стоимость (без кэша) | Стоимость (с кэшем) |
|-----------|---------------------|---------------------|
| Daily Summary | 30₽ | ~10₽ |
| RAG queries | 25₽ | ~8₽ |
| Embeddings | 2₽ | 2₽ |
| **ИТОГО** | **57₽** | **~20₽/месяц** |

### Qwen Turbo (самый дешёвый вариант)

| Компонент | Input | Output | Стоимость |
|-----------|-------|--------|-----------|
| Daily Summary | 1.2M × 4₽ | 0.12M × 17₽ | 5₽ + 2₽ = **7₽** |
| RAG queries | 0.9M × 4₽ | 0.15M × 17₽ | 4₽ + 3₽ = **7₽** |
| Embeddings | 1.1M × 1.6₽ | — | **2₽** |
| **ИТОГО** | | | **16₽/месяц** |

### GPT-4o Mini (надёжный fallback)

| Компонент | Input | Output | Стоимость |
|-----------|-------|--------|-----------|
| Daily Summary | 1.2M × 12₽ | 0.12M × 48₽ | 14₽ + 6₽ = **20₽** |
| RAG queries | 0.9M × 12₽ | 0.15M × 48₽ | 11₽ + 7₽ = **18₽** |
| Embeddings | 1.1M × 1.6₽ | — | **2₽** |
| **ИТОГО** | | | **40₽/месяц** |

---

## Сводная таблица (5 групп)

| Модель | Стоимость/месяц | Цензура | Качество RU | Вердикт |
|--------|-----------------|---------|-------------|---------|
| **DeepSeek V3.2-Exp** | **57₽** | 🟢 Минимальная | ⭐⭐⭐⭐⭐ | ✅ Лучший баланс |
| Qwen Turbo | **16₽** | 🟢 Низкая | ⭐⭐⭐ | Экономия, но проще |
| GPT-4o Mini | **40₽** | 🟡 Средняя | ⭐⭐⭐⭐ | Надёжный fallback |
| Mistral Devstral Small | **25₽** | 🟢 Низкая | ⭐⭐⭐ | Для кода лучше |
| Gemini 2.5 Flash | **25₽** | 🔴 Высокая | ⭐⭐⭐⭐ | ❌ Будет резать |

---

## Масштабирование

| Групп | Сообщений/день | DeepSeek V3.2 | Qwen Turbo | GPT-4o Mini |
|-------|----------------|---------------|------------|-------------|
| 5 | 750 | 57₽ | 16₽ | 40₽ |
| 10 | 1,500 | 114₽ | 32₽ | 80₽ |
| 25 | 3,750 | 285₽ | 80₽ | 200₽ |
| 50 | 7,500 | 570₽ | 160₽ | 400₽ |
| 100 | 15,000 | 1,140₽ | 320₽ | 800₽ |

---

## API провайдеры

| Провайдер | DeepSeek V3.2 | Комиссия | Регистрация |
|-----------|---------------|----------|-------------|
| **OpenRouter** | ✅ | 5.5% | openrouter.ai |
| DeepSeek напрямую | ✅ | 0% | platform.deepseek.com |
| Together AI | ✅ | ~5% | together.ai |

**Рекомендация:** OpenRouter — уже настроен в проекте, 500+ моделей, легко переключаться.

---

## Оптимизация стоимости

### 1. Используй кэширование промптов
```
System prompt одинаковый → cache hit → экономия 90% на input
```

### 2. Группируй сообщения
```
Вместо 150 отдельных сообщений → 1 запрос с контекстом
```

### 3. Batch API (если не срочно)
```
OpenAI Batch: -50% на embeddings
DeepSeek: поддерживает batching
```

### 4. Умное RAG
```
Не эмбеддить каждое сообщение
Группируй по временным окнам (5-10 минут)
```

---

## Итоговая рекомендация

### Для 5 групп: **DeepSeek V3.2-Exp**

| Параметр | Значение |
|----------|----------|
| Стоимость | **~57₽/месяц** (с кэшем ~20₽) |
| Цензура | Минимальная |
| Русский язык | Отличный |
| Контекст | 128K токенов |
| Провайдер | OpenRouter (уже настроен) |

### Настройка в проекте

Изменить в `appsettings.json`:
```json
{
  "Kimi": {
    "Model": "deepseek/deepseek-chat-v3-0324"
  }
}
```

Или через OpenRouter:
```json
{
  "Kimi": {
    "BaseUrl": "https://openrouter.ai/api",
    "Model": "deepseek/deepseek-chat"
  }
}
```

---

## Инструкция: Подключение DeepSeek V3.2

### Шаг 1: Регистрация на DeepSeek

1. Перейди на **https://platform.deepseek.com/**
2. Нажми **"Sign Up"** (или войди через Google/GitHub)
3. Подтверди email

### Шаг 2: Пополнение баланса

1. В личном кабинете перейди в **"Billing"** → **"Top Up"**
2. **Минимальный депозит: $5** (~400₽)
3. Способы оплаты:
   - Карта Visa/Mastercard (иностранная)
   - Криптовалюта (USDT, BTC)

> **Проблема с российскими картами:**
> DeepSeek напрямую может не принять карты РФ.
>
> **Решение:** Используй **OpenRouter** — принимает больше способов оплаты.

### Шаг 3: Получение API ключа

1. Перейди в **"API Keys"**
2. Нажми **"Create API Key"**
3. Скопируй ключ (показывается только 1 раз!)
4. Сохрани в безопасное место

### Шаг 4: Настройка в проекте

В `appsettings.json` (или `appsettings.Development.json`):

```json
{
  "Kimi": {
    "ApiKey": "sk-xxxxxxxxxxxxxxxxxxxxxxxx",
    "BaseUrl": "https://api.deepseek.com",
    "Model": "deepseek-chat"
  }
}
```

---

## Альтернатива: OpenRouter (проще для РФ)

### Почему OpenRouter лучше для России

- Принимает **криптовалюту** без ограничений
- Работает с **PayPal** (через VPN)
- Один ключ → доступ к 500+ моделям
- Уже настроен в твоём проекте!

### Шаг 1: Регистрация

1. Перейди на **https://openrouter.ai/**
2. Нажми **"Sign In"** → Google/GitHub/Email
3. Подтверди аккаунт

### Шаг 2: Пополнение баланса

1. Перейди в **"Credits"** → **"Add Credits"**
2. **Минимум: $5** (~400₽)
3. Способы оплаты:
   - **Криптовалюта** (USDT, ETH, BTC) — лучший вариант для РФ
   - Карта (может потребоваться иностранная)
   - PayPal

### Шаг 3: Получение API ключа

1. Перейди в **"Keys"**
2. Нажми **"Create Key"**
3. Дай имя (например: "WatchmenBot")
4. Скопируй ключ `sk-or-v1-xxxxx`

### Шаг 4: Настройка в проекте

```json
{
  "Kimi": {
    "ApiKey": "sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxx",
    "BaseUrl": "https://openrouter.ai/api",
    "Model": "deepseek/deepseek-chat"
  }
}
```

### Доступные модели DeepSeek на OpenRouter

| Модель | ID для конфига |
|--------|----------------|
| DeepSeek V3 | `deepseek/deepseek-chat` |
| DeepSeek R1 (reasoning) | `deepseek/deepseek-r1` |
| DeepSeek Coder | `deepseek/deepseek-coder` |

---

## Инструкция: Настройка RAG

### Что нужно для RAG

| Компонент | Решение | Стоимость |
|-----------|---------|-----------|
| Vector Database | **pgvector** (PostgreSQL) | **Бесплатно** |
| Embedding модель | **text-embedding-3-small** | ~2₽/месяц |

### Шаг 1: Установка pgvector

pgvector — бесплатное расширение для PostgreSQL. У тебя уже есть PostgreSQL!

**Для Docker (рекомендуется):**
```bash
# Используй образ с pgvector
docker pull pgvector/pgvector:pg16

# Или добавь в docker-compose.yml:
services:
  postgres:
    image: pgvector/pgvector:pg16
    # ... остальные настройки
```

**Для локальной установки (Windows):**
1. Скачай pgvector: https://github.com/pgvector/pgvector/releases
2. Скопируй файлы в папку PostgreSQL
3. Перезапусти PostgreSQL

**Для облачного PostgreSQL:**
- **Supabase** — pgvector включён бесплатно
- **Neon** — pgvector включён бесплатно
- **AWS RDS** — нужно включить расширение

### Шаг 2: Активация расширения

Выполни SQL:
```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

### Шаг 3: Создание таблицы для эмбеддингов

```sql
-- Таблица для хранения эмбеддингов сообщений
CREATE TABLE message_embeddings (
    id BIGSERIAL PRIMARY KEY,
    chat_id BIGINT NOT NULL,
    message_id BIGINT NOT NULL,
    chunk_text TEXT NOT NULL,
    embedding vector(1536),  -- размерность text-embedding-3-small
    metadata JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW(),

    UNIQUE(chat_id, message_id)
);

-- Индекс для быстрого поиска похожих векторов
CREATE INDEX idx_embeddings_vector
ON message_embeddings
USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 100);

-- Индекс для фильтрации по чату
CREATE INDEX idx_embeddings_chat_id
ON message_embeddings(chat_id);
```

### Шаг 4: Получение API ключа OpenAI (для embeddings)

1. Перейди на **https://platform.openai.com/**
2. Зарегистрируйся или войди
3. Перейди в **"API Keys"** → **"Create new secret key"**
4. Скопируй ключ `sk-xxxxx`

**Пополнение:**
- Минимум **$5** (~400₽)
- Карта Visa/Mastercard (иностранная) или
- Через посредников (криптовалюта)

### Шаг 5: Настройка в проекте

Добавь в `appsettings.json`:
```json
{
  "Embeddings": {
    "ApiKey": "sk-xxxxxxxxxxxxxxxxxxxxxxxx",
    "Model": "text-embedding-3-small",
    "BaseUrl": "https://api.openai.com/v1"
  }
}
```

---

## Итоговая стоимость настройки

| Компонент | Единоразово | Ежемесячно |
|-----------|-------------|------------|
| DeepSeek/OpenRouter депозит | ~400₽ ($5) | — |
| OpenAI депозит (embeddings) | ~400₽ ($5) | — |
| pgvector | Бесплатно | Бесплатно |
| **Использование (5 групп)** | — | **~60₽** |

### Итого для старта: ~800₽

Этого хватит на **6-12 месяцев** работы для 5 групп!

---

## Чек-лист настройки

- [ ] Зарегистрироваться на OpenRouter (или DeepSeek напрямую)
- [ ] Пополнить баланс ($5 минимум)
- [ ] Получить API ключ
- [ ] Обновить `appsettings.json` с новым ключом и моделью
- [ ] Установить pgvector в PostgreSQL
- [ ] Создать таблицу `message_embeddings`
- [ ] Зарегистрироваться на OpenAI
- [ ] Получить ключ для embeddings
- [ ] Добавить секцию `Embeddings` в конфиг
- [ ] Протестировать на одной группе

---
